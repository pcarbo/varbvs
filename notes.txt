eStrategy for my new implementation of 'varbvs'
----------------------------------------------

* Options: Summary statistics from "fixed" variational estimates for
  an external set of variables.
	
* Give platform, R version and MATLAB version used for testing.

* For PLoS Comp. Bio. software section, see:
  doi:10.1371/journal.pcbi.1002799
  Example article: doi:10.1371/journal.pcbi.1004085

* To do: in summary/print function, add note about coefficients in
  logistic regression not being strictly interpretable

* To do: Implement varbvspredict.m.

* To do: add description/man page for varbvspredict/predict.varbvs.

* To do: Create R script to compare cv.glmnet and varbvs in leukemia
  data.

* To do: Map QTLs using GEMMA using simple linear model.

* To do: Add function that outputs prior variance (sa) given X, prior
  logodds and PVE (h^2).

* To do: Allow specification of summary statistics ('Xb') from "fixed" 
  variational estimates for an external set of variables.

* To do: Point to extended examples in vignettes and/or github.

