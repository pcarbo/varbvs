varbvs.qtl	  Using varbvs to map a quantitative trait in a simulated
                  genetic data set.
varbvs.cc	  Using varbvs to map a binary (case-control) trait in a
                  simulated genetic data set.
varbvs.resid.vcov Illustrates how the support for two varbvs models can be 
                  compared by computing a Bayes factor (using varbvsbf).
varbvsmix	  Using the varbvsmix function on a simulated data set.
varbvsmix.dense   Illustrates the "drop.threshold" option in varbvsmix
                  to speed up computation when a lot of the mixture
                  components have near-zero weight.
varbvsmix.test    Verifies that varbvs and varbvsmix produce the same
                  result when there are exactly 2 mixture components.
